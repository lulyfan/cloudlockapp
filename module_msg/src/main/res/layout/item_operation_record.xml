<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="operationRecord"
            type="com.ut.module_msg.model.OperationRecord" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:background="@color/white"
        android:paddingLeft="@dimen/base_20dp"
        android:paddingRight="@dimen/base_20dp"
        android:layout_marginBottom="@dimen/base_2dp"
        android:elevation="2dp">

        <ImageView
            android:id="@+id/stick"
            android:layout_width="@dimen/base_40dp"
            android:layout_height="@dimen/base_40dp"
            android:src="@mipmap/default_user_icon"
            app:url="@{operationRecord.icon}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="16dp"
            android:layout_marginRight="16dp"
            android:textSize="@dimen/base_14sp"
            android:textColor="@color/color_base_title"
            android:text="@{operationRecord.operator}"
            app:layout_constraintBottom_toTopOf="@id/desc"
            app:layout_constraintLeft_toRightOf="@id/stick"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{operationRecord.desc}"
            app:layout_constraintBottom_toBottomOf="parent"
            android:textSize="@dimen/base_12sp"
            android:textColor="@color/gray9"
            app:layout_constraintLeft_toLeftOf="@id/name"
            app:layout_constraintTop_toBottomOf="@id/name" />
    </android.support.constraint.ConstraintLayout>
</layout>
